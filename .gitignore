# Source art dump (icons already extracted into assets/classes/) — the
# wallpapers alone are 150MB+ and were never meant to be committed.
4) Class Icon, Wallpaper/

# Generated by scripts/seed-from-repo.js — a one-time D1 migration snapshot,
# regenerate with `node scripts/seed-from-repo.js` rather than committing it
# (it goes stale the moment D1 diverges from data.js/profiles.js/attendance.js).
seed.sql

# Third-party CoGM Logger app + a real session capture (159MB+ .pcap, personal
# Discord/family-name data) — used locally to investigate a Kill Map feature
# (see memory/project_kill_map_investigation.md), never meant to be committed.
CoGM/
